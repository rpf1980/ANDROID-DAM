<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apppizeria_v02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_personalizada"></activity>
        <activity android:name=".ActivityFavorita" />
        <activity
            android:name=".ActivityPedido"
            android:parentActivityName=".ActivityOpcionesPizzeria">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityOpcionesPizzeria" />
        </activity>
        <!--
        Del activity donde queremos pulsar y volver atrás,
            hay que indicarle cual será su padre. Es decir, hacia donde queremos que nos lleve.
            No olvidemos añadir también el metadata
        -->
        <activity
            android:name=".ActivityOpcionesPizzeria"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>